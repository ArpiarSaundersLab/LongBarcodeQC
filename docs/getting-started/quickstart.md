# Quickstart

Minimal arguments using the default assembly plasmid.

```bash
LongBarcodeQC -i <input_fastq_directory> \
    -o <output_directory> \
    -b <barcode_fasta_file> \
    -l <expected_cassette_insertion_length>
```

